version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./backups:/app/backups
      - ./.env:/app/.env
    environment:
      - FLASK_ENV=production
